# -- Database (PostgreSQL) ---------------------------------------------------
# A connection URL in the form:
# postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://bandsite:bandsite_pw@localhost:5433/bandsitepro_dev"

# -- NextAuth.js -------------------------------------------------------------
# Base URL of your frontend (no trailing slash)
NEXTAUTH_URL=http://localhost:3000
# A long, random string (32+ chars)
NEXTAUTH_SECRET=e9b52db55e72e4666a623ccabd429f9b26c1260873274a00069ace3a85ace62e

# -- Spotify API (for Music embeds) ------------------------------------------
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=

# -- Stripe & Medusa (for Merch) ---------------------------------------------
# Your Stripe secret key (starts with sk_)
STRIPE_API_KEY=
# Your Stripe webhook secret (for verifying events)
STRIPE_WEBHOOK_SECRET=

# (If you run Medusa on a separate port or host, point to it:)
# MEDUSA_BACKEND_URL=http://localhost:9000

# -- Cloudinary (if you use it for media storage) ----------------------------
# cloudinary://API_KEY:API_SECRET@CLOUD_NAME
CLOUDINARY_URL=

EMAIL_SERVER_HOST=smtp.ethereal.email
EMAIL_SERVER_PORT=587
EMAIL_SERVER_USER=noemy.runte37@ethereal.email
EMAIL_SERVER_PASSWORD=phSGRAJPQhCgKmzxeY
EMAIL_FROM=“BandSitePro <no-reply@ethereal.email>”